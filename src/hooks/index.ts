/**
 * ============================================================================
 * Custom Hooks Index
 * ============================================================================
 * 
 * Barrel export for all custom React hooks.
 * 
 * Usage: import { useMobile, useToast } from '@/hooks';
 * 
 * Available Hooks:
 * - useMobile: Detects mobile screen size
 * - useToast: Toast notification management
 * - useSessionBehavior: User session tracking
 * - useTypingPlaceholder: Animated typing placeholder
 * ============================================================================
 */

export { useIsMobile } from './use-mobile';
export { useToast } from './use-toast';
export { useSessionBehavior } from './useSessionBehavior';
export { useTypingPlaceholder } from './useTypingPlaceholder';
